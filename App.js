import React from 'react';
import AppNavigator from './src/navigation/AppNavigator';

// Keep App minimal — navigation is handled entirely in AppNavigator
export default function App() {
  return <AppNavigator />;
}
